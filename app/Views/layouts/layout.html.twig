<!DOCTYPE html>
<html lang="nl">
    <head>
        <title>JOOO</title>
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    </head>
    <body>
    <nav class="relative bg-zinc-200 shadow-xl after:pointer-events-none after:absolute after:inset-x-0 after:bottom-0 after:h-px after:bg-white/10">
        <div class="relative flex mx-24 h-16 items-center">
            <div class="flex flex-1 items-center justify-between">
                <a class="flex shrink-0 items-center" href="/">
                    <img src="joo" alt="lul" class="h-8 w-auto" />
                </a>
                <div class="hidden sm:ml-6 sm:block">
                    <div class="flex space-x-4">
                            {% for item in navigation %}
                                <a href='{{ item.href }}' aria-current="page" class="rounded-md bg-neutral-400 hover:bg-blue-500 duration-300 px-3 py-2 text-sm font-medium text-white">{{ item.caption }}</a>
                            {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <main id="content"{% block content %}{% endblock %}</main>
    </body>
</html>